# 象棋辅助工具 - 完整使用指南

## 🎯 最终目标

编译出一个 APK 安装包，安装到手机后可以：
1. ✅ 离线使用（不需要网络）
2. ✅ 悬浮窗显示 AI 建议
3. ✅ 自动识别其他象棋 APP 的棋盘
4. ✅ 使用 Pikafish 引擎分析（和皮卡鱼一样强）

## 📦 需要准备的文件

### 关键文件：Pikafish 引擎（ARM 版本）

你需要获取两个文件：
1. `pikafish-arm64` - 64位手机用
2. `pikafish-armv7` - 32位手机用

### 获取方式

#### 方式 1：自己编译（需要技术）
参考 `编译Pikafish引擎.md`

#### 方式 2：从网上下载（推荐）
- 搜索 "Pikafish Android ARM"
- 或者从其他象棋 APP 中提取
- 或者使用 Termux 在手机上编译

#### 方式 3：使用替代引擎
如果实在找不到 Pikafish，可以用其他象棋引擎：
- ElephantEye（象眼）
- Cyclone（旋风）
- 这些引擎可能有 Android 版本

## 📂 文件放置位置

获取到引擎文件后，放在这里：

```
XiangqiAssistant_Android/
└── app/
    └── src/
        └── main/
            └── assets/
                ├── pikafish-arm64    ← 64位引擎
                └── pikafish-armv7    ← 32位引擎
```

**创建 assets 文件夹**（如果没有）：
1. 在 Android Studio 中
2. 右键点击 `app/src/main`
3. New → Directory
4. 输入 `assets`

## 🔨 编译流程

### 步骤 1：准备引擎文件

```
✅ 获取 pikafish-arm64
✅ 获取 pikafish-armv7
✅ 放入 app/src/main/assets/ 文件夹
```

### 步骤 2：打开项目

1. 启动 Android Studio
2. Open → 选择 `XiangqiAssistant_Android`
3. 等待 Gradle 同步

### 步骤 3：编译 APK

**方法 A：直接运行到手机**
1. 连接手机（USB 调试）
2. 点击绿色运行按钮
3. 等待编译和安装

**方法 B：生成 APK 文件**
1. Build → Build Bundle(s) / APK(s) → Build APK(s)
2. 等待编译完成
3. 找到 APK：`app/build/outputs/apk/debug/app-debug.apk`

## 📱 安装和使用

### 安装

1. 将 APK 复制到手机
2. 点击安装
3. 允许"未知来源"安装

### 首次使用

1. **打开 APP**
2. **授予权限**：
   - 悬浮窗权限 ✅
   - 截屏权限 ✅
   - 存储权限 ✅

3. **启动服务**：
   - 点击"启动悬浮窗"
   - 点击"开始屏幕识别"

4. **使用悬浮窗**：
   - 打开其他象棋 APP（如天天象棋、象棋巫师等）
   - 悬浮窗会自动识别棋盘
   - 显示最佳走法

### 悬浮窗功能

- **拖动**：长按悬浮窗可以移动位置
- **分析**：点击"分析"按钮手动分析
- **关闭**：点击 X 按钮关闭悬浮窗

## ⚠️ 如果没有引擎文件怎么办？

### 方案 A：使用模拟模式

如果 `assets` 文件夹中没有引擎文件，APP 会自动进入**模拟模式**：
- ✅ 所有功能正常
- ✅ 悬浮窗正常显示
- ⚠️ 走法是模拟的（不是真实 AI 分析）
- ⚠️ 显示"(模拟)"标记

**模拟模式可以用来测试 APP 功能**，等找到引擎文件后再替换。

### 方案 B：后续添加引擎

1. 编译 APK（不带引擎）
2. 安装到手机
3. 后续找到引擎文件后：
   - 重新放入 `assets` 文件夹
   - 重新编译 APK
   - 覆盖安装

## 🔍 常见问题

### Q1：找不到 Pikafish ARM 版本怎么办？

**答**：
1. 先编译不带引擎的版本（模拟模式）
2. 测试其他功能是否正常
3. 慢慢寻找或编译引擎
4. 或者使用其他象棋引擎替代

### Q2：编译后 APK 很大怎么办？

**答**：
- 引擎文件通常 5-10MB
- 加上 APP 本身，总大小 20-30MB
- 这是正常的

### Q3：悬浮窗识别不准确？

**答**：
1. 确保棋盘清晰可见
2. 调整手机亮度
3. 当前识别算法较简单，需要优化
4. 可以手动点击"分析"按钮

### Q4：引擎分析很慢？

**答**：
- 手机性能有限
- 可以降低分析深度（修改代码中的 depth 参数）
- 或者使用更快的引擎

### Q5：能在模拟器上运行吗？

**答**：
- 可以，但模拟器是 x86 架构
- 需要 x86 版本的引擎
- 或者使用 ARM 模拟器（很慢）

## 📊 功能对比

| 功能 | 有引擎 | 无引擎（模拟） |
|------|--------|----------------|
| 悬浮窗 | ✅ | ✅ |
| 屏幕识别 | ✅ | ✅ |
| AI 分析 | ✅ 真实 | ⚠️ 模拟 |
| 离线使用 | ✅ | ✅ |
| 棋力 | 🔥 强 | ❌ 无 |

## 🎓 学习建议

### 如果你是新手

1. **先编译模拟版本**
   - 不需要引擎文件
   - 测试 APP 基本功能
   - 熟悉编译流程

2. **学习如何获取引擎**
   - 研究 Pikafish 编译
   - 或寻找现成的 ARM 版本

3. **集成真实引擎**
   - 替换引擎文件
   - 重新编译
   - 测试分析功能

### 如果你有技术基础

1. 直接编译 Pikafish ARM 版本
2. 放入 assets 文件夹
3. 一次性编译完整版本

## 🚀 进阶功能

### 自定义引擎

可以替换成其他象棋引擎：
- 修改 `PikafishEngine.kt`
- 适配不同的 UCI 命令
- 支持多种引擎切换

### 优化识别算法

- 训练深度学习模型识别棋子
- 支持更多棋盘样式
- 提高识别准确率

### 添加更多功能

- 棋谱保存
- 开局库查询
- 残局练习
- 在线对弈

## 📞 需要帮助？

如果遇到问题：
1. 查看 `编译教程.md`
2. 查看 `编译Pikafish引擎.md`
3. 检查 Android Studio 的错误信息
4. 搜索相关错误解决方案

## ⚖️ 法律声明

- 本工具仅供学习研究使用
- 请勿用于在线对弈作弊
- 使用本工具的一切后果由使用者承担
- 开发者不承担任何法律责任

---

**祝你编译成功！享受象棋的乐趣！** 🎉♟️
