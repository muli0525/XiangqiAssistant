# 象棋辅助工具 - 详细编译教程

## 📋 准备工作

### 1. 下载安装 Android Studio

1. 访问官网：https://developer.android.com/studio
2. 下载最新版本（推荐 2023.1 或更高）
3. 安装时选择标准安装（Standard）
4. 等待下载 SDK 组件（需要几个 GB，耐心等待）

### 2. 配置 Android Studio

首次启动后：
1. 选择主题（随意）
2. 等待初始化完成
3. 进入欢迎界面

## 🔧 导入项目

### 步骤 1：打开项目

1. 在 Android Studio 欢迎界面点击 **"Open"**
2. 找到 `XiangqiAssistant_Android` 文件夹
3. 点击 **"OK"**

### 步骤 2：等待 Gradle 同步

- 首次打开会自动下载依赖（需要 5-15 分钟）
- 底部会显示进度条
- 如果失败，点击 **"Sync Project with Gradle Files"** 重试

### 步骤 3：检查 SDK

1. 点击菜单 **Tools** → **SDK Manager**
2. 确保已安装：
   - ✅ Android 14.0 (API 34)
   - ✅ Android SDK Build-Tools 34.0.0
   - ✅ Android SDK Platform-Tools
   - ✅ Android NDK

如果没有，勾选后点击 **"Apply"** 安装。

## 📱 连接手机

### 方法 A：使用真机（推荐）

1. **开启开发者选项**
   - 进入手机 **设置** → **关于手机**
   - 连续点击 **"版本号"** 7 次
   - 提示"您已处于开发者模式"

2. **开启 USB 调试**
   - 返回设置，找到 **开发者选项**
   - 开启 **USB 调试**
   - 开启 **USB 安装**（部分手机有）

3. **连接电脑**
   - 用 USB 线连接手机和电脑
   - 手机上会弹出"允许 USB 调试"，点击 **允许**
   - Android Studio 顶部工具栏会显示你的手机型号

### 方法 B：使用模拟器

1. 点击菜单 **Tools** → **Device Manager**
2. 点击 **"Create Device"**
3. 选择 **Pixel 6** 或其他设备
4. 选择系统镜像（推荐 API 34）
5. 点击 **"Finish"**
6. 启动模拟器

## 🔨 编译 APK

### 方法 1：直接运行（推荐新手）

1. 确保手机已连接（或模拟器已启动）
2. 点击顶部工具栏的 **绿色三角形按钮（Run）**
3. 等待编译（首次需要 10-30 分钟）
4. 编译完成后会自动安装到手机

### 方法 2：生成 APK 文件

1. 点击菜单 **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. 等待编译完成
3. 底部会显示 **"locate"** 链接，点击打开文件夹
4. APK 位置：`app/build/outputs/apk/debug/app-debug.apk`
5. 将这个 APK 复制到手机安装

### 方法 3：使用命令行（高级）

打开项目根目录的终端：

**Windows:**
```cmd
gradlew.bat assembleDebug
```

**Mac/Linux:**
```bash
./gradlew assembleDebug
```

## ⚠️ 常见问题

### 问题 1：Gradle 同步失败

**错误信息**：`Could not resolve all dependencies`

**解决方法**：
1. 检查网络连接
2. 点击 **File** → **Settings** → **Build, Execution, Deployment** → **Gradle**
3. 勾选 **"Offline work"**（离线模式）
4. 或者配置代理

### 问题 2：编译失败 - SDK 版本问题

**错误信息**：`Failed to find target with hash string 'android-34'`

**解决方法**：
1. 打开 **SDK Manager**
2. 安装 Android 14.0 (API 34)
3. 重新同步项目

### 问题 3：手机无法识别

**解决方法**：
1. 检查 USB 线是否支持数据传输
2. 重新安装手机驱动（Windows）
3. 尝试其他 USB 接口
4. 手机上重新授权 USB 调试

### 问题 4：安装失败 - 签名问题

**错误信息**：`INSTALL_FAILED_UPDATE_INCOMPATIBLE`

**解决方法**：
1. 卸载手机上的旧版本
2. 重新安装

### 问题 5：OpenCV 依赖下载失败

**解决方法**：
1. 打开 `app/build.gradle`
2. 注释掉 OpenCV 依赖：
   ```gradle
   // implementation 'org.opencv:opencv:4.8.0'
   ```
3. 重新同步

## 📦 安装到手机

### 方法 A：通过 Android Studio

编译完成后会自动安装。

### 方法 B：手动安装 APK

1. 将 `app-debug.apk` 复制到手机
2. 手机上找到这个文件
3. 点击安装
4. 如果提示"禁止安装未知应用"：
   - 点击 **设置**
   - 允许该来源安装应用

## 🎯 首次使用

### 1. 授予权限

打开 APP 后需要授予：
- ✅ 悬浮窗权限
- ✅ 截屏权限
- ✅ 存储权限

### 2. 启动服务

1. 点击 **"启动悬浮窗"**
2. 点击 **"开始屏幕识别"**
3. 打开其他象棋 APP
4. 悬浮窗会显示 AI 建议

## 💡 提示

### 编译时间

- **首次编译**：10-30 分钟（下载依赖）
- **后续编译**：1-5 分钟

### 电脑配置要求

- **最低**：8GB 内存，100GB 硬盘空间
- **推荐**：16GB 内存，SSD 硬盘

### 加速编译

1. 关闭杀毒软件（临时）
2. 使用 SSD 硬盘
3. 增加 Gradle 内存：
   编辑 `gradle.properties`：
   ```
   org.gradle.jvmargs=-Xmx4096m
   ```

## 🆘 需要帮助？

如果遇到问题：
1. 查看 Android Studio 底部的 **"Build"** 标签页，查看详细错误
2. 复制错误信息，搜索解决方案
3. 检查 README.md 中的常见问题

## ✅ 编译成功标志

看到以下信息表示成功：
```
BUILD SUCCESSFUL in 2m 34s
```

APK 文件位置：
```
app/build/outputs/apk/debug/app-debug.apk
```

---

**祝你编译顺利！如有问题随时查阅本教程。** 🎉
